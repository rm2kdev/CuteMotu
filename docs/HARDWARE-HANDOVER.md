# USB hardware handover

Follow the current [installation guide](INSTALL.md), especially the existing-driver and first-install steps. The installer requires `--backend usb --hardware-handover` for physical USB access. Keep SIP enabled.
